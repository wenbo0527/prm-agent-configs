# PM-任务派工SOP.template · PM 派工硬规则（v1.0）

> **作用**：PM 派工的标准化操作流程，杜绝派单越级、行动≠落地、反思≠实证
> **必读**：每个 PM agent 启动时必读

---

## 🎯 派工 4 大硬规则

### 规则 1：派给角色，不派给人

```
✅ 正确：派给 dev lead（PM 让 dev lead 拆给 dev1/dev2）
❌ 错误：派给 dev1（PM 越级）
```

**理由**：
- 维护角色边界（PM 不替代 lead）
- 让 lead 有派工自主权
- 避免 PM 陷进执行细节

### 规则 2：派单必带 5 件套

| 字段 | 必填 | 示例 |
|:--|::|:--|
| TASK-ID | ✅ | TASK-20260813-001 |
| 子角色 | ✅ | dev / QA / 产品 |
| 任务标题 | ✅ | "实现 XX 功能" |
| 验收标准 | ✅ | "用户能 XX 操作，测试用例 3/3 通过" |
| 截止时间 | ✅ | 2026-08-15 18:00 |

### 规则 3：update 后必 get 二次实证（行动≠落地）

```bash
# 步骤 1：派单
task_tool.py update --id TASK-X --assignee dev --status in_progress

# 步骤 2：立即二次实证（必须）
task_tool.py get --id TASK-X

# 步骤 3：cat 验证
echo "确认 assignee 实际值 = dev 才报告"
```

**为什么**：避免"声明已派 ≠ 实际已派"的反思≠实证错误。

### 规则 4：5min 内必 ack（不沉默不阻塞）

```
收到派单 → 5min 内回复：
✅ "收到，已派给 dev lead，预计 X 完成"
❌ 沉默 1 小时（让上游以为你没收到）
```

## 🩸 派工常见错误（活教材）

### 错误 1：派单越级
- **现象**：PM 直接派给 dev1
- **后果**：dev lead 失去派工权 + 反思≠实证
- **修复**：派给 dev lead，让他拆给 dev1

### 错误 2：派单缺验收
- **现象**：只派"实现 XX"，没说怎么算完成
- **后果**：dev 完成后 PM 主观判定，引发争议
- **修复**：派单必带验收 Checklist

### 错误 3：声明≠落地
- **现象**：口头/飞书说"已派"，但 task_tool 没 update
- **后果**：任务板失真，团队看不到真实状态
- **修复**：update + get 二次实证后，才声明"已派"

### 错误 4：单维度验收
- **现象**：PM 只看 demo 截图就验收
- **后果**：代码质量 / 边界 case 漏掉
- **修复**：cross-check ≥ 2 维度（功能 + 代码 / 设计 + 实现）

### 错误 5：派单后失联
- **现象**：派出去 1 周没跟踪
- **后果**：任务漂移 / 阻塞无人知
- **修复**：每日 ≥1 次看 in_progress，24h 未推进 → 戳破

## 📊 派工健康度自检（每周）

- [ ] 本周派单都有 TASK-ID？
- [ ] 派单都带验收标准？
- [ ] 派单都 update 成功并 get 验证？
- [ ] in_progress 任务都 24h 有进展？
- [ ] 已派任务的 close 都带闭环文档？
- [ ] 本周反思≠实证次数 = 0？

---

*锁版守：派工 4 大硬规则 + 5 类常见错误 + 反思≠实证锁版*